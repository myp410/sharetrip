<table class="table">
  <thead>
    <tr>
      <th>グループ名</th>
      <th>紹介文</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% groups.each do |f| %>
      <tr>
        <td><%= link_to group.name, group_path(group) %></td>
        <td><%= group.introduction %></td>
        <% if group.owner_id == current_user.id %>
          <td>
            <%= link_to "編集", edit_group_paht(group), class: "btn btn-sm btn-success" %>
          </td>
        <% end %>
      </tr>
    <% end %>  
  </tbody>
</table>