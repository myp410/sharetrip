<% if obj.errors.any? %>
  <div class="alert alert-danger text-center">
    <h4><%= obj.errors.count %> 件のエラーが発生しました</h4>
    <ul class="list-unstyled">
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>